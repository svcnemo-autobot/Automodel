# Copyright (c) 2026, NVIDIA CORPORATION. All rights reserved.
#
# StepFun Step3.7/Step3.5-VL MedPix LoRA fine-tune.
# 8-node LoRA layout: 64 GPUs = PP8 x DP8, with EP8 over the DP shard.
# global_batch_size=64 and local_batch_size=8 keep gradient accumulation at 1.

recipe: FinetuneRecipeForVLM

seed: 1234

step_scheduler:
  global_batch_size: 64
  local_batch_size: 8
  ckpt_every_steps: 1000
  val_every_steps: 25
  log_remote_every_steps: 1
  gc_every_steps: 10
  max_steps: 100

dist_env:
  backend: nccl
  timeout_minutes: 90

rng:
  _target_: nemo_automodel.components.training.rng.StatefulRNG
  seed: 1234
  ranked: true

model:
  _target_: nemo_automodel.NeMoAutoModelForImageTextToText.from_pretrained
  pretrained_model_name_or_path: stepfun-ai/Step-3.7-Flash
  trust_remote_code: false
  torch_dtype: bfloat16
  backend:
    _target_: nemo_automodel.components.models.common.BackendConfig
    attn: te
    linear: te
    rms_norm: torch_fp32
    rope_fusion: false
    experts: gmm
    dispatcher: deepep
    fake_balanced_gate: false
    enable_hf_state_dict_adapter: true
    enable_fsdp_optimizations: true

processor:
  _target_: nemo_automodel.components.models.step3p7.processing_step3.Step3VLProcessor.from_pretrained
  pretrained_model_name_or_path: stepfun-ai/Step-3.7-Flash
  trust_remote_code: false

peft:
  _target_: nemo_automodel.components._peft.lora.PeftConfig
  match_all_linear: false
  exclude_modules:
    - "*vision_tower*"
    - "*vision_model*"
    - "*vision*"
    - "*visual*"
    - "*image_encoder*"
    - "*lm_head*"
    - "*audio*"
  dim: 32
  alpha: 32
  dropout: 0.0
  moe_rank_scaling: true

checkpoint:
  enabled: false
  checkpoint_dir: vlm_checkpoints/step3p7_medpix_lora_pp8ep8_8node/
  model_save_format: safetensors
  save_consolidated: false

distributed:
  strategy: fsdp2
  tp_size: 1
  cp_size: 1
  pp_size: 8
  ep_size: 8
  sequence_parallel: false
  activation_checkpointing: true
  pipeline:
    pp_schedule: interleaved1f1b
    pp_microbatch_size: 1
    layers_per_stage: 2
    round_virtual_stages_to_pp_multiple: up
    scale_grads_in_schedule: false
    patch_inner_model: false
    patch_causal_lm_model: false
  moe:
    reshard_after_forward: false
    wrap_outer_model: true

freeze_config:
  freeze_embeddings: true
  freeze_vision_tower: true
  freeze_audio_tower: true
  freeze_language_model: false

loss_fn:
  _target_: nemo_automodel.components.loss.masked_ce.MaskedCrossEntropy
  fp32_upcast: false

dataset:
  _target_: nemo_automodel.components.datasets.vlm.datasets.make_medpix_dataset
  path_or_dataset: mmoukouba/MedPix-VQA
  split: train

dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  num_workers: 2
  persistent_workers: true
  pin_memory: true
  collate_fn:
    _target_: nemo_automodel.components.datasets.vlm.collate_fns.default_collate_fn
    max_length: 2048
  drop_last: true

validation_dataset:
  _target_: nemo_automodel.components.datasets.vlm.datasets.make_medpix_dataset
  path_or_dataset: mmoukouba/MedPix-VQA
  split: validation[:256]

validation_dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  num_workers: 1
  collate_fn:
    _target_: nemo_automodel.components.datasets.vlm.collate_fns.default_collate_fn
    max_length: 2048

optimizer:
  _target_: torch.optim.Adam
  betas: [0.9, 0.999]
  eps: 1e-8
  lr: 1.0e-4
  weight_decay: 0

lr_scheduler:
  lr_decay_style: cosine
  min_lr: 1.0e-6

wandb:
  enable: false
  project: <your_wandb_project>
  entity: <your_wandb_entity>
  name: <your_wandb_name>
  dir: <your_wandb_save_dir>

ci:
  # pp_size(8) * ep_size(8) = 64 GPUs => 8 nodes (8 H100/node).
  nodes: 8
  # The complete source, reload, and resume matrix repeatedly constructs this 200B model.
  time: "02:00:00"
  checkpoint_robustness:
    # PP=8 with pp_microbatch_size=1 requires at least eight pipeline microbatches.
    step_scheduler.global_batch_size: 64
    step_scheduler.local_batch_size: 8
    # One optimizer step materializes Adam state; AutoModel setup then restores it from DCP.
    step_scheduler.max_steps: 1
    step_scheduler.ckpt_every_steps: 1
    # Process isolation is the harness default, so each large trainer exits before the next one starts.
    # Vanilla HF needs remote model code and shards the 403 GB base over rank 0's local GPU node.
    trust_remote_code: true
    hf_device_map_auto: true
    # Keep overflow in RAM and leave GPU headroom for PEFT's adapter re-dispatch.
    hf_device_map_max_memory_gib: 55
    hf_device_map_cpu_max_memory_gib: 128
    # CPU-offloaded rank-0 HF reload can exceed the shared 30-minute wait on a busy cluster.
    hf_reload_timeout_seconds: 3600
    # The published HF model intentionally omits training-only MTP layers 45-47. Exact-match every
    # adapter tensor HF can instantiate, while allowing only the saved MTP adapter namespace.
    hf_adapter_ignored_key_prefix: "base_model.model.mtp."
    # HF is not self-repeatable, so only the two HF-based numerical gates are
    # informational. AutoModel reload remains blocking. Across three exact-state
    # scoped runs its mean KL varied from 0.00447 to 0.03215 while within-process
    # self-repeat stayed bitwise zero. Keep the shared relaxed profile and override
    # only the two Phase 2 gates exceeded by this model; relaxed p95 remains active.
    parity_tolerance_profile: relaxed
    parity_threshold_overrides:
      automodel_reload:
        mean_kl: 0.04
        cosine_similarity: 0.99
    skip_source_load_logit_parity: true
    skip_hf_reload_logit_parity: true
    # Restored model/optimizer state and the first forward are exact. The
    # process-sensitive MoE path diverges only after computing the next update.
    resume_tolerance_profile: relaxed
    tokenizer_name: stepfun-ai/Step-3.7-Flash
